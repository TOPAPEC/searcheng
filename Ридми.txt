Иногда появляется 'WordNetCorpusReader' object has no attribute '_LazyCorpusLoader__args',
но у меня это каждый раз фиксилось перезагрузкой страницы 

Датасет отсюда https://www.reddit.com/r/datasets/comments/1uyd0t/200000_jeopardy_questions_in_a_json_file/
В коде из текста вырезаются все ссылки. Перед индексацией и векторизацией применяется лемматизация, удаление стоп слов.

Еще нужен crawl-300d-2M.vec отсюда https://fasttext.cc/docs/en/english-vectors.html 
В проге грузятся только первые 300к, потому что пайчарм, в отличие от юпитера, не использовал всю оперативку почему-то и падал

Весь код из search.py также есть в ava.ipynb 

Часть файлов генерируется при выполнении ноутбука, а потом просто используется в серче. 